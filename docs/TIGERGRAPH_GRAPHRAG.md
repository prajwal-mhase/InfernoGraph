# TigerGraph GraphRAG Integration

This submission includes TigerGraph-facing artifacts for production handoff:

- Graph schema: `graph_pipeline/tigergraph_schema.gsql`
- Route and evidence behavior represented in backend graph retrieval and compression flows.

The local submission backend runs in in-memory mode for deterministic judging, while preserving schema-level compatibility for TigerGraph deployment.
