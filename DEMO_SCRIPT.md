# Demo Script

1. Start backend: `./start_backend.ps1`
2. Start frontend: `./start_frontend.ps1`
3. Open dashboard at `http://127.0.0.1:5500/dashboard.html`
4. Run the default biomedical question.
5. Compare `LLM Only`, `Basic RAG`, and `InfernoGraph` on tokens, cost, latency, and judge score.
